apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: ep-auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: ep_auth
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: ep-auth-secret
                  key: DB_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ep-auth-secret
                  key: DB_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: ep-auth
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
